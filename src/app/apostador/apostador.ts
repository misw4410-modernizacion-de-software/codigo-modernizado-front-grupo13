export class Apostador {
    id: number;
    nombreApostador: string;

    constructor(
        id: number,
        nombreApostador: string
    ) {
        this.id = id,
            this.nombreApostador = nombreApostador
    }
}
